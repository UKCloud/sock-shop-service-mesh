apiVersion: v1
kind: Service
metadata:
  name: catalogue
  labels:
    name: catalogue
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: tcp
    port: 80
    targetPort: 80
  selector:
    name: catalogue
