apiVersion: v1
kind: Service
metadata:
  name: catalogue
  labels:
    app: catalogue
    service: catalog
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 80
  selector:
    app: catalogue
