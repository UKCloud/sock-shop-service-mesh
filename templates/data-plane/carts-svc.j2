apiVersion: v1
kind: Service
metadata:
  name: carts
  labels:
    name: carts
  namespace: {{ data_plane_namespace }}
spec:
  ports:
    # the port that this service should serve on
  - protocol: TCP
    name: tcp
    port: 80
    targetPort: 80
  selector:
    name: carts
