apiVersion: v1
kind: Service
metadata:
  name: orders
  labels:
    name: orders
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: tcp
    port: 80
    targetPort: 80
  selector:
    name: orders
