apiVersion: v1
kind: Service
metadata:
  name: orders
  labels:
    app: orders
    service: orders
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 80
  selector:
    app: orders
