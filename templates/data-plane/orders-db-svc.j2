apiVersion: v1
kind: Service
metadata:
  name: orders-db
  labels:
    app: orders-db
    service: orders-db
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: mongodb
    port: 27017
    targetPort: 27017
  selector:
    app: orders-db
