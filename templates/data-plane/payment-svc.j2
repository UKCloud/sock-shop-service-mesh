apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    app: payment
    service: payment
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: http
    port: 80
    targetPort: 80
  selector:
    app: payment
