apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    name: payment
  namespace: {{ data_plane_namespace }}
spec:
  ports:
    # the port that this service should serve on
  - protocol: TCP
    name: tcp
    port: 80
    targetPort: 80
  selector:
    name: payment
