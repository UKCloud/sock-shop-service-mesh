apiVersion: v1
kind: Service
metadata:
  name: carts-db
  labels:
    name: carts-db
    app: carts
    service: carts-db
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - name: mongo
    protocol: TCP
    port: 27017
  selector:
    name: carts-db
