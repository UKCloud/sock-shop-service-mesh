apiVersion: v1
kind: Service
metadata:
  name: shipping
  labels:
    app: shipping
    service: shipping
  namespace: {{ data_plane_namespace }}
spec:
  ports:
  - protocol: TCP
    name: tcp
    port: 80
    targetPort: 80
  selector:
    app: shipping
